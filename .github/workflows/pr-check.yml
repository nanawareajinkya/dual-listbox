# ===================================================================
# 🧪 Pull Request Quality Check for @nanawareajinkya/dual-listbox
# Runs on PRs to main — lightweight lint validation
# ===================================================================

name: PR Check

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint Check (Pre-Merge)
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout PR code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      # 3️⃣ Install dependencies (fast mode)
      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      # 4️⃣ Run lint (non-blocking warnings)
      - name: Run ESLint
        run: |
          npm run lint --if-present || echo "✅ Non-blocking lint warnings — safe to merge."

      # 5️⃣ Status summary
      - name: Done
        run: echo "✨ Pre-merge lint check completed successfully!"
