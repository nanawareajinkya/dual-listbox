# ===================================================================
# Continuous Integration for @nanawareajinkya/dual-listbox
# Built and maintained by Ajinkya Nanaware
# ===================================================================

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build & Verify
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x] # ✅ Angular 20 compatible Node version

    steps:
      # -------------------------------------------------------------
      # 1️⃣ Checkout repository
      # -------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -------------------------------------------------------------
      # 2️⃣ Setup Node.js + npm caching
      # -------------------------------------------------------------
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      # -------------------------------------------------------------
      # 3️⃣ Install dependencies (handle peer conflicts safely)
      # -------------------------------------------------------------
      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      # -------------------------------------------------------------
      # 4️⃣ Lint (non-blocking)
      # -------------------------------------------------------------
      - name: Run ESLint (non-blocking)
        run: |
          npm run lint --if-present || echo "✅ Lint warnings ignored (no blocking issues found)."

      # -------------------------------------------------------------
      # 5️⃣ Build Angular library
      # -------------------------------------------------------------
      - name: Build Angular library
        run: npm run build:lib

      # -------------------------------------------------------------
      # 6️⃣ Run unit tests (skipped — library-only project)
      # -------------------------------------------------------------
      - name: Skip tests
        run: echo "🧪 No tests configured — skipping test phase."

      # -------------------------------------------------------------
      # 7️⃣ Upload compiled dist folder for inspection
      # -------------------------------------------------------------
      - name: Upload dist artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: dual-listbox-dist
          path: dist/dual-listbox/

      # -------------------------------------------------------------
      # 8️⃣ Auto-publish on version tag push (optional)
      # -------------------------------------------------------------
      # - name: Publish to npm
      #   if: startsWith(github.ref, 'refs/tags/v')
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      #   run: npm publish --access public
